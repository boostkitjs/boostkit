---
"create-rudder-app": patch
---

Add `@rudderjs/boost` to the package multiselect as an opt-in devDependency. Surfaces a `rudder boost:install` hint in the post-scaffold "Done!" output so users can wire their AI coding assistant (Claude Code / Cursor / Copilot / etc.) to project internals via MCP.
